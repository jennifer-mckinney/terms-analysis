# .claude/ Audit — 2026-07-31

Executed post-PR-#87 (78afa2b + 5 Copilot autofix commits) on branch `claude/2026-07-31-issue-triage-and-audit`.
Head at audit time: `2619c1e` (fix(migrate-script): remove dead _vendor_from_url alias).

## Summary

- Files audited: 15 (`.claude/CLAUDE.md`, 10 `library/LIB-*.md`, 2 `rules/*.md`, `AGENTS.md` root, `_governance-manifest.json`)
- MATCHES CURRENT: 6
- NEEDS EDIT: 6
- RETIRE / STALE: 1 (`AGENTS.md` root — out of scope, recommend only)
- Governance drift: 1 (`_governance-manifest.json` expected; Task 7 regen)

## Per-file findings

### `.claude/CLAUDE.md`

#### SO1 — 873 tests / 98.06% coverage: **NEEDS EDIT**
- Verification: `cd src/backend && source .venv/bin/activate && pytest --collect-only -q | tail -5` → **828 tests collected**
- Verification: `pytest --cov=app --cov-report=term -q | tail -5` → **828 passed, TOTAL 98%** (2187 statements, 53 missed → 97.577% precise)
- Discrepancy: SO1 says 873; actual 828 (some tests were consolidated or removed post-PR-#34; coverage still 98% within rounding)
- Correction: change `873 tests passing, 98.06% coverage` → `828 tests passing, 98% coverage` OR keep as historical snapshot but re-anchor via a new SO covering the current state.

#### SO2 — IRP fields on Finding: **MATCHES CURRENT**
- Verification: `grep -n "impact|likelihood|safeguard_score|irp_score" src/backend/app/schemas.py` → lines 172-175 (all four fields present with expected constraints).

#### SO3 — 5 chips: **MATCHES CURRENT**
- Verification: `grep` in `src/backend/app/schemas.py` lines 140-144 → all 5 chip values present (`want_understand`, `for_child`, `for_care`, `for_work`, `just_curious`).

#### SO4 — 4 domains: **MATCHES CURRENT**
- Verification: `grep 'Data use|Terms of use|Privacy rights|_DOMAIN' src/backend/app/services/analyzer.py` → `_DOMAIN_MAP` at line 37; sections for Data, Data use, Terms of use, Privacy rights all present with ~50 category mappings.

#### SO5 — empty jurisdictions no-filter: **MATCHES CURRENT**
- Verification: `src/backend/app/services/rules.py:1039` `jurisdiction_filter = set(jurisdictions) if jurisdictions else None`; `src/backend/app/services/analyzer.py:877` same pattern → confirms empty-list disables filter.

#### SO6 — schema-derived allowlists: **MATCHES CURRENT**
- Verification: `src/backend/app/main.py:65` `_VALID_CHIPS = frozenset(get_args(ContextChip))`; `main.py:66` `_VALID_JURISDICTIONS`; `schemas.py:50` `CATEGORIES: frozenset[str] = frozenset({...})`; `context.py:125` import-time guard `if cat not in CATEGORIES`; `analyzer.py:102` `_unknown_domain_keys` guard. All fires at import.

#### SO7 — streamlit v2 feature flag: **MATCHES CURRENT**
- Verification: `src/webapp/app_streamlit_v2.py` exists; `src/webapp/app_streamlit_legacy.py` exists; `run.sh:22` `STREAMLIT_UI="${STREAMLIT_UI:-v2}"`.

#### SO8 — POST /infer: **MATCHES CURRENT**
- Verification: `grep '@app.post("/infer"' src/backend/app/main.py` → line 358.

#### SO9 — test_regressions_pr34.py 30 tests: **NEEDS EDIT**
- Verification: `pytest tests/test_regressions_pr34.py --collect-only -q | tail -3` → **42 tests collected** across 10 classes (`TestCategoryA` through `TestCategoryG` + `TestJurisdictionFilterBoundary`, `TestCategoryH`, `TestCategoryI`).
- Correction: change `30 tests` → `42 tests`; add Categories H (schema validator edges) and I (inference edges) beyond A-G listed.

#### SO10 — shell-native test scripts: **MATCHES CURRENT**
- Verification: `ls scripts/testing/simplification-check.sh scripts/testing/smoke-test.sh` → both present.

#### SO11-SO13 — P9 loop / revamp shipped / hard-gate mirrored: **MATCHES CURRENT**
- Verification: `.githooks/pre-push` present; `automations/p9-pre-push.md` present with mirror provenance header (`commit 3fb017e on 2026-07-04`).

#### SO14-SO16 — ingester project + Phase 0.1 + CI venv fix: **MATCHES CURRENT (with drift)**
- Verification: sibling repo `~/Documents/.../legal-corpus-ingester/` present. Recent HEAD: `e184b8a fix(ci): add mypy ignore_missing_imports for yaml and lxml stubs` (moved past `459a1b8` claimed in SO15; SO16 references `9db68f3`; both are historical anchors and remain accurate as such).
- Note: SO14-16 are historical — no edit needed, but a follow-up SO documenting the current ingester HEAD would keep the chain live.

#### project-map row for `tests/`: **NEEDS EDIT (closes #75)**
- Verification: `ls tests/` returns `__init__.py`, `test_api_endpoints.py`, `test_batch_analysis.py`, `test_child_context_simplification.py`, `test_quick_mode.py` (4 test files).
- Correction: add row `| \`tests/\` | Root integration/E2E tests separate from unit tests in \`src/backend/tests/\`; 4 files: api_endpoints, batch_analysis, child_context_simplification, quick_mode |`

#### project-map "24 endpoints + /health": **NEEDS EDIT**
- Verification: `grep -cE '@app\.(get|post|put|delete|patch)' src/backend/app/main.py` → **26** total decorators (25 business + `/health`). Post-SO8, `/infer` was added making it 25 business routes.
- Correction: change `24 endpoints + /health` → `25 endpoints + /health`.

### `.claude/library/LIB-API.md`

#### Endpoint count status line: **NEEDS EDIT**
- Line 6: `status (2026-07-03): endpoint map = 24 business routes + /health = 25 total`
- Actual: 25 business + `/health` = 26 total (adds `/infer` per SO8).
- Correction: `endpoint map = 25 business routes + /health = 26 total`.
- Also: endpoint table at lines 10-35 lists `/health`, `/analyze`, `/analyze/url`, `/analyze/file`, `/analyze/batch`, `/analyses`, `/rubric`, `/analyses/{id}`, `/exports/analyses.csv`, `/exports/analysis/{id}.pdf`, `/exports/analysis/{id}`, `/reviews`, `/reviews/{id}`, `/watchlist` GET, `/watchlist` POST, `/watchlist/{id}` DELETE, `/watchlist/{id}/refresh`, `/snapshots` GET, `/snapshots/detail/{id}`, `/snapshots` POST, `/diff/{id1}/{id2}`, `/policy-watch` POST, `/policy-watch` GET, `/policy-watch/{id}` DELETE, `/policy-watch/{id}/snapshot` = 25 rows including `/health`. `/infer` is MISSING from the table.
- Correction: add row for `POST /infer` between `/health` and `/analyze` with request/response contracts per SO8 + `schemas.InferResponse`.

### `.claude/library/LIB-ARCH.md`

#### C2 endpoint count: **NEEDS EDIT**
- Line 10 (C2 row): `24 REST endpoints + /health`
- Actual: 25 business + /health.
- Correction: `25 REST endpoints + /health`.

### `.claude/library/LIB-STACK.md`

#### requirements.txt parity: **NEEDS EDIT**
- Missing from LIB-STACK python-deps table (present in requirements.txt): `pytest` (already listed under test-deps — OK), `PyYAML` (added recently, absent from table), `sqlalchemy` (present — OK).
- Actual `requirements.txt` includes: fastapi, httpx, beautifulsoup4, pytesseract, pillow, pypdf, python-docx, python-dotenv, python-multipart, pytest, reportlab, sqlalchemy, striprtf, uvicorn, langdetect, rank_bm25, numpy, **PyYAML**.
- LIB-STACK table is otherwise present-and-correct. Note: `pydantic` is in table but not requirements.txt (transitive via FastAPI — acceptable, but worth noting).
- Correction: add `| PyYAML | MIT | A+ | YAML config parsing |` row to LIB-STACK python-deps table.

### `.claude/library/LIB-TEST.md`

#### baseline test count "702 tests": **NEEDS EDIT**
- Line 6: `baseline is **702 tests, 98.06% line coverage**`
- Actual: 828 tests, 98% line coverage (post PR #87 + Copilot autofixes).
- Correction: `baseline is **828 tests, 98% line coverage**` (or anchor to a new SO date).

#### test file inventory missing 7 files: **NEEDS EDIT**
- LIB-TEST §baseline lists 17 test files; actual filesystem has 24 non-`__init__` test files.
- Missing from LIB-TEST table: `test_audit_phase1_fixes.py`, `test_critical_p9_fixes.py`, `test_intake_form_race.py`, `test_legal_kb_bundle.py`, `test_phantom_alias_removed.py`, `test_vendor_derivation_surface_spec.py`, `test_watchlist_merge.py`.
- Correction: add rows for each; audit doc lists filename only — brief single-line coverage summary should be added by Task 7 executor after skimming each file's docstring.

#### regressions row "30 tests": **NEEDS EDIT**
- Lines 26 and 36 both say `30 tests`. Actual is 42.
- Correction: `42 tests` (see SO9 finding above).

### `.claude/library/LIB-CONTEXT.md`

#### Reference to `app_streamlit_v2.py`: **MATCHES CURRENT**
- Line 6, 10, 218: correctly reference `app_streamlit_v2.py`. No stale bare `app_streamlit.py`.

### `.claude/library/LIB-LEGAL.md`

#### FAISS/Meta refs and numpy exhaustive claim: **MATCHES CURRENT**
- Lines 74, 91, 139: correctly reject FAISS/torch/sentence-transformers; state numpy exhaustive is used.

### `.claude/library/LIB-PRINCIPLES.md`

#### FAISS/Meta line: **MATCHES CURRENT**
- Line 57 correctly reflects hard-requirement HR2.

### `.claude/library/LIB-RULES.md`, `LIB-EVAL.md`, `LIB-VOICE.md`
- Not audited in depth this pass (no obvious drift signals from cross-referencing SO1-16 or code); recommend spot-check in a future audit round if new SO claims land.

### `.claude/rules/testing.md`

#### Reference implementation imports: **MATCHES CURRENT**
- `_VALID_CHIPS` in `src/backend/app/main.py:65` ✓
- `_VALID_JURISDICTIONS` in `src/backend/app/main.py:66` ✓
- `ContextChip` in `src/backend/app/schemas.py` (Literal type) ✓
- `Jurisdiction` in `src/backend/app/schemas.py` (Literal type) ✓
- `get_args` from `typing` (stdlib) ✓
- All reference impls in `testing.md` R1/R2/R3 sections compile against current code.

### `.claude/rules/code-style.md`
- No factual claims to verify against code (pure convention doc). MATCHES CURRENT.

### `.claude/_governance-manifest.json`

#### Hash drift expected: **NEEDS REGEN (Task 7)**
- Verification: `bash scripts/governance/verify-hashes.sh; echo exit: $?` → exit 1
- `.claude/CLAUDE.md`: expected `6ff926d1b642` got `bc94477a914a` (+4912 bytes; SO14-16 added in earlier session)
- `.claude/library/LIB-PRINCIPLES.md`: expected `a5c0c7a362a6` got `f9569fd20ac3` (+1657 bytes)
- Action: Task 7 will re-apply Task 6 edits then run `scripts/governance/regen-manifest.sh --yes` to reset baseline.

### `AGENTS.md` (repo root — NOT in .claude/, flagged by plan)

#### Staleness confirmed: **RETIRE / STALE (out of scope for Task 7)**
- Verification: `wc -l AGENTS.md` → 34 lines.
- Verification: `grep -cE 'app\.js|<script>|<!DOCTYPE|streamlit|Streamlit|app_streamlit' AGENTS.md` → 4 matches (all Streamlit-positive — good) BUT:
  - Line 6: `src/demos/`: "demo/prototype HTML files (multiple versions)" — src/demos/ current status?
  - Line 20: **"No automated test framework is configured."** — FALSE. `src/backend/tests/` has 828 pytest tests at 98% coverage.
  - Line 21: **"Do manual smoke checks in the browser after changes"** — misleading; project has pytest + shell test scripts + P9 pre-push gate.
  - Line 22: **"Some demos include self-test/validation buttons (e.g., v6/v7 demos)"** — refers to retired HTML demo era.
  - Line 17: "HTML/JS use 4 spaces; CSS uses 2 spaces" — reflects retired HTML/JS webapp; current stack is Python/Streamlit per `.claude/rules/code-style.md`.
  - Line 33: "This is a client-only app" — FALSE. Current stack has FastAPI backend on :9000.
- Recommendation: **REWRITE** or **archive as `AGENTS.md.retired.md`**. Do NOT edit in this task (out of scope; needs a separate anchor to BRD/PRD and a fresh PRD-sourced narrative). Recommend opening a follow-up issue.

## Recommended edits (Task 7 will execute)

Task 6 (this task) applies these edits directly per Step 10. Task 7 commits + regenerates governance manifest.

1. `.claude/CLAUDE.md` project-map: change `24 endpoints + /health` → `25 endpoints + /health`; add row for root `tests/`.
2. `.claude/CLAUDE.md` SO1: change `873 tests passing, 98.06% coverage` → `828 tests passing, 98% coverage`.
3. `.claude/CLAUDE.md` SO9: change `30 tests` → `42 tests`; expand Category coverage note to include H (schema-validator edges) and I (inference edges) beyond A-G.
4. `.claude/library/LIB-API.md`: change status line `24 business routes ... = 25 total` → `25 business routes ... = 26 total`; add `POST /infer` row to endpoint table.
5. `.claude/library/LIB-ARCH.md`: change C2 row `24 REST endpoints + /health` → `25 REST endpoints + /health`.
6. `.claude/library/LIB-STACK.md`: add `PyYAML` row to python-deps table.
7. `.claude/library/LIB-TEST.md`: change baseline `702 tests, 98.06%` → `828 tests, 98%`; regressions rows `30 tests` → `42 tests` (both occurrences); add rows for 7 missing test files.

## Recommended archives / rewrites (out of scope)

- `AGENTS.md` root: FULL REWRITE required. Current file describes a retired HTML/JS demo era. Anchor to BRD (`docs/BRD_Terms_Policies_Reviewer.md`) + PRD (`docs/PRD_Terms_Policies_Reviewer.md`) + current `.claude/CLAUDE.md` project-map. Recommend opening a new issue with this audit finding as evidence.

## BRD/PRD contradictions

None found in the audited files. BRD/PRD were not read cover-to-cover this pass; recommend a dedicated BRD/PRD-vs-code audit next session (out of scope for Task 6).
